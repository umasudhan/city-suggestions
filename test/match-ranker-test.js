const sinon = require('sinon');
const expect = require('chai').expect;
const assert = require('chai').assert;

const MatchRanker = require('../lib/match-ranker');

describe('Match Ranker', function(){
    it('orders matches first by distance from user lat long', function(){
    });

    it('orders inexact matches by distance', function(){
    });

    it('specifies rank for each result', function(){
    });
});